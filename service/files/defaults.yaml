configs:
  horizon:
    port: 80
    secret_key: secret

    wsgi:
      processes: 6
      threads: 15

sources:
  openstack/horizon:
    git_url: https://github.com/openstack/horizon.git
    git_ref: stable/newton
